# 1.12.0 README meaning gate

Every public release theme appears in all four localized READMEs. The needles below are checked by [README-CONTENT-CHECKS-1.12.0.env](README-CONTENT-CHECKS-1.12.0.env).

| Public theme | English | Korean | Japanese | Simplified Chinese | Release-notes-only reason |
| --- | --- | --- | --- | --- | --- |
| Realtime Plus is selectable alongside the retained Qwen3.8 model | `qwen-audio-3.1-realtime-plus`; `qwen3.8-livetranslate-flash-realtime` | 동일 모델 ID 2개 | 同左 | 同左 | None |
| Async Filetrans tool and exact model | `qwen-audio-3.1-asr-flash-filetrans` | 동일 모델 ID | 同左 | 同左 | None |
| Workspace ID is required only for Qwen3.8; Audio 3.1 Realtime Plus and Filetrans use the key without it | `Qwen3.8 LiveTranslate also requires a workspace ID`; `Qwen Audio 3.1 Realtime Plus and Filetrans use the key without one` | `Qwen3.8 LiveTranslate는 워크스페이스 ID도 필요`; `Qwen Audio 3.1 Realtime Plus와 Filetrans는 키만 사용` | `Qwen3.8 LiveTranslateにはワークスペースIDも必要`; `Qwen Audio 3.1 Realtime PlusとFiletransはキーだけで利用` | `Qwen3.8 LiveTranslate 还需要工作空间 ID`; `Qwen Audio 3.1 Realtime Plus 和 Filetrans 仅使用密钥` | None |
| Filetrans accepts a public HTTPS URL; QwenCloud fetches it, with no local-file upload | `Only a public HTTPS audio URL is accepted`; `QwenCloud fetches its audio, and AirTranslate does not upload a local file` | `공개 HTTPS 오디오 URL만 지원`; QwenCloud가 URL에서 오디오를 가져옴 | `公開HTTPS音声URLのみ利用できます`; QwenCloudがURLから音声を取得 | `仅接受公开的 HTTPS 音频 URL`; QwenCloud 会从 URL 获取音频 | None |
| Qwen Audio 3.1 rates are account-specific and should be checked in Model Studio | `Current Qwen Audio pricing is shown in Model Studio` | `Qwen Audio 요금은 Model Studio에서 확인` | `Qwen Audioの料金はModel Studioで確認` | `请在 Model Studio 中查看当前 Qwen Audio 价格` | None |

The `README_CONTENT_CHECKS` list includes one localized needle for each theme in every README. The release heading and download version are covered separately by the update-set audit.
