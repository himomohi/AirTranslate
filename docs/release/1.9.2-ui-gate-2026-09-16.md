# AirTranslate 1.9.2 UI gate — Issue #14

Status: **PASS for the actual approval, cancellation, retry, installation and static translation checks below.**

## Before: 1.9.1 actual UI reproduction

- Running canonical `dist/AirTranslate.app`, Apple mode, English → Korean, PC audio, translation output, voice off, floating captions off, Ready.
- Read-only `LanguageAvailability` probe: English → Korean/Japanese installed; English → Chinese Simplified/French/German/Italian/Spanish/Portuguese/Ukrainian/Vietnamese supported but not installed. `TranslationSession(installedSource:target:).canRequestDownloads` returned `false`.
- Computer Use selected English → Chinese Simplified and opened Settings > Assets. Translation Language Pack showed Download Required.
- Clicking Translation Language Pack Download immediately changed it to `Unable to Translate` / Check Failed / Retry. No language-download approval dialog appeared.
- Restored English → Korean and the original Ready state. No existing language pack was removed; no key or TCC setting changed.

## Acceptance checklist for final app

- Request a missing translation language pair through the macOS approval UI.
- Cancel, retry, approve, complete download, and verify Installed status through the app and read-only LanguageAvailability.
- Preserve already-installed language behavior; reject stale completions after language changes; prevent duplicate requests.
- Verify translated content with newly installed assets if safely available, separate from live audio/provider proof.
- Verify keyboard/focus/labels for any new progress/cancel UI and restore original app settings.
- Record final version/build, executable path, source commit, and verified archive hash.

## After: 1.9.2 actual UI and local translation

- Implementation commit: `5b3cd5d35c5ee74b03878d73eef72878e71aa909`. Canonical `./script/build_and_run.sh --verify` passed, exit 0 (release compile 34.40 seconds), with `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer`, `CODE_SIGN_IDENTITY=-`, and `BUILD_CONFIGURATION=release`.
- Running PID 26703 was exactly `dist/AirTranslate.app/Contents/MacOS/AirTranslate`. Its Info.plist reported version `1.9.2`, build `192`, identifier `dev.appcaster.AirTranslate`.
- Computer Use selected the same previously missing English → Chinese Simplified pair. Settings > Assets > Translation Language Pack Download now opened Apple's **Download Languages to Translate** sheet, showing **English (US)** and **Chinese (Mandarin, Simplified)**. The old immediate Unable to Translate error did not occur.
- Cancellation/retry: Escape dismissed the Apple sheet. The app's independent progress window remained cancellable; clicking **취소** closed the request and restored Download Required. Retrying the same pair opened a fresh Apple sheet successfully.
- Clicked the official sheet's Download button. Both language rows showed progress. Done returned to the app's progress window; the app kept waiting until installation finished, then closed the window and showed **번역 언어팩 설치됨**. The download action did not start audio capture.
- A read-only `LanguageAvailability` probe returned `en -> zh-Hans asset status: installed`. A local installed-session translation of the fixed, non-sensitive sentence `The weather is pleasant today.` returned `今天天气很好。` and `STATIC_SENTENCE_TRANSLATION_PASS`. This exercised the newly installed translation assets rather than a mocked session.
- Additional keyboard cancellation: selected the missing English → French pair, opened the Apple approval sheet, dismissed it with Escape, then pressed Escape in the app's progress window. The window closed and Settings returned to Download Required. French was not downloaded in this check.
- Accessibility observations: the request window exposed the current language pair, a labelled progress indicator, a **취소** button, and a native close button. Escape activated cancellation. VoiceOver speech, full Tab traversal and contrast measurements were not performed.
- Restored Apple Mode, English → Korean, PC audio, translation output, voice off and floating captions off. Main window displayed Ready/준비됨. No existing language pack was deleted; no provider key, TCC permission or audio capture setting was changed.

## Evidence and boundaries

- Build log: `/tmp/airtranslate-issue14-canonical-app-build.log`.
- Local read-only probes: `/tmp/airtranslate-issue14-language-baseline.swift` and `/tmp/airtranslate-issue14-translation-probe.swift`.
- Environment: macOS 27.0 (26A428), Xcode 27.0, Swift 6.4. This is not proof of the same UI on macOS 26; CI is recorded separately.
- Apple's Done/Escape can dismiss its sheet while an OS download continues in the background. The app intentionally checks installation and keeps a visible Cancel action; app cancellation does not promise to stop OS-managed background bytes.
- Late completion, duplicate requests, language/mode change, Stop and quit ownership are covered by the focused and full automated tests. Manual closing of the main/settings window during an active download was not separately exercised.
- Live system-audio capture, SpeechAnalyzer asset installation and provider transactions were not exercised. The speech-recognition pack still reports Download Required independently of successful translation-pack installation.
- Final distribution archive identity and remote download hashes are recorded in the test/security/publication reports after packaging.

## Final packaged app delivery

After the artifact verifier reported archive checks PASS, the app was quit and the previous UI-tested bundle was preserved in `/tmp/airtranslate-issue14-local-backup-20260916/AirTranslate-1.9.2-ui-verified.app`. The exact `Release/product/AirTranslate.app` was copied to `dist`, and recursive file comparison plus strict codesign verification passed. Computer Use reopened this packaged app and confirmed the restored Ready state with Apple / English → Korean / PC audio / translation / voice off / floating off. The verified executable was PID `40320` at `dist/AirTranslate.app/Contents/MacOS/AirTranslate`. Packaging did not change product source.
